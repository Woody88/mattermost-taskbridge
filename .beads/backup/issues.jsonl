{"acceptance_criteria":"","actor":"","agent_state":"","assignee":null,"await_id":"","await_type":"","close_reason":"Fixed by docker/entrypoint.sh wrapper that pre-starts a long-lived bd dolt server per cloned repo before taskbridge begins serving requests. Clean pod boot now produces /projects Open:4 Ready:4, /board returns 4 attachment cards, /task show returns rich detail. Pushed as commit 412f45f on Woody88/mattermost-taskbridge, image rebuilt and deployed to the homelab cluster.","closed_at":"2026-04-07T16:55:28Z","closed_by_session":"","compacted_at":null,"compacted_at_commit":null,"compaction_level":0,"content_hash":"4fddfdcdd33d0fe49dde02f3f8cfe663e3562ca5493665a349ca377f7e0d11e6","created_at":"2026-04-07T06:24:02Z","created_by":"Woodson Delhia","crystallizes":0,"defer_until":null,"description":"When mattermost-taskbridge runs in the homelab cluster, the slash command handler invokes bd via BunChildProcessSpawner per-request. bd auto-starts dolt on first call, but subsequent calls hang or fail because of dolt-server.lock state and the fact that each bd invocation is a fresh subprocess racing over the same project. Symptoms observed in production: (1) bd list works once via kubectl exec, (2) the next bd list --json --status open from the slash command handler returns nothing (handler swallows the error), (3) bd doctor inside the pod hangs. Root cause: dolt is designed as a long-lived SQL server, not a per-request invocation. Fix candidates: (a) start bd dolt as a long-lived process via the container entrypoint (wrapper script that does 'cd \u003crepo\u003e \u0026\u0026 bd dolt start \u0026' before exec'ing taskbridge), (b) run dolt as a sidecar container in the same pod with an emptyDir for the unix socket, (c) switch beads to no-db mode (no-db: true in .beads/config.yaml) which uses JSONL only and skips dolt entirely \\u2014 simpler but uncommitted feature, (d) use an initContainer that runs 'bd dolt start' against the PVC. Discovered while bringing up the first homelab deployment of obsidian-mcp-server-oyd. Workaround for now: deploy is structurally correct but /board returns 0/0 in cluster until this is solved.","design":"","due_at":null,"ephemeral":0,"estimated_minutes":null,"event_kind":"","external_ref":null,"hook_bead":"","id":"obsidian-mcp-server-290","is_template":0,"issue_type":"bug","last_activity":null,"metadata":"{}","mol_type":"","no_history":0,"notes":"","original_size":null,"owner":"woodsondelhia@gmail.com","payload":"","pinned":0,"priority":1,"quality_score":null,"rig":"","role_bead":"","role_type":"","sender":"","source_repo":"","source_system":"","spec_id":"","status":"closed","target":"","timeout_ns":0,"title":"bd-in-container dolt server lifecycle: handler can't read after manual exec leaves stale lock","updated_at":"2026-04-07T16:55:28Z","waiters":"","wisp_type":"","work_type":""}
{"acceptance_criteria":"","actor":"","agent_state":"","assignee":"Woodson Delhia","await_id":"","await_type":"","close_reason":"Project-local rename done: package.json, README, CLAUDE.md vault section, .claude/settings.json hook, bd-sync-reminder.ts file. Directory rename and global obgo-sync hook deferred pending user confirmation.","closed_at":"2026-04-07T03:51:49Z","closed_by_session":"","compacted_at":null,"compacted_at_commit":null,"compaction_level":0,"content_hash":"ddb016cf2be72d01d9c092c50a1a752d5e287c27c8c709a901d08e3994dd6ad6","created_at":"2026-04-07T00:59:50Z","created_by":"Woodson Delhia","crystallizes":0,"defer_until":null,"description":"Strip Obsidian vault references, rename package, replace README, remove bd-sync-reminder hook. Repo is being reframed as a Bun service that bridges Beads task state into Mattermost via slash commands and interactive messages.","design":"","due_at":null,"ephemeral":0,"estimated_minutes":null,"event_kind":"","external_ref":null,"hook_bead":"","id":"obsidian-mcp-server-2pv","is_template":0,"issue_type":"chore","last_activity":null,"metadata":"{}","mol_type":"","no_history":0,"notes":"","original_size":null,"owner":"woodsondelhia@gmail.com","payload":"","pinned":0,"priority":1,"quality_score":null,"rig":"","role_bead":"","role_type":"","sender":"","source_repo":"","source_system":"","spec_id":"","status":"closed","target":"","timeout_ns":0,"title":"Repurpose repo from obsidian-mcp-server to mattermost-taskbridge","updated_at":"2026-04-07T03:51:49Z","waiters":"","wisp_type":"","work_type":""}
{"acceptance_criteria":"","actor":"","agent_state":"","assignee":"Woodson Delhia","await_id":"","await_type":"","close_reason":"ktp: spawner.string→spawn+exitCode in Git.ts (test/git.test.ts is the regression). 2xp: chain lefthook from .beads/hooks/pre-commit (outside markers); removed lefthook install from prepare. oyd: smoke confirmed from Mattermost. wpq, r98: ADR records, work landed in 412f45f / bacdbbc / earlier — closing as recorded.","closed_at":"2026-04-07T18:28:55Z","closed_by_session":"","compacted_at":null,"compacted_at_commit":null,"compaction_level":0,"content_hash":"656a2829785745ab298870d938f4c2fba0805a3ced76b0746f87e86e75dfa232","created_at":"2026-04-07T04:32:36Z","created_by":"Woodson Delhia","crystallizes":0,"defer_until":null,"description":"Running 'bun install' or 'bun add' triggers the prepare script which calls 'lefthook install'. This fails with: 'core.hooksPath is set locally to .beads/hooks'. Beads claims git core.hooksPath for its own hooks; lefthook refuses to install on top of that. Workaround: 'bun add --ignore-scripts'. Real fix is one of: (a) call 'lefthook install --force' (clobbers beads hooks - bad), (b) wrap both hook systems via a multiplexer script in .beads/hooks/, (c) drop lefthook in favor of beads-only hooks, (d) drop the lefthook install from prepare and run it manually only when needed. Discovered while installing @effect/platform-bun for the Phase 1 MVP.","design":"","due_at":null,"ephemeral":0,"estimated_minutes":null,"event_kind":"","external_ref":null,"hook_bead":"","id":"obsidian-mcp-server-2xp","is_template":0,"issue_type":"bug","last_activity":null,"metadata":"{}","mol_type":"","no_history":0,"notes":"","original_size":null,"owner":"woodsondelhia@gmail.com","payload":"","pinned":0,"priority":2,"quality_score":null,"rig":"","role_bead":"","role_type":"","sender":"","source_repo":"","source_system":"","spec_id":"","status":"closed","target":"","timeout_ns":0,"title":"Lefthook prepare-script fails due to beads core.hooksPath conflict","updated_at":"2026-04-07T18:28:55Z","waiters":"","wisp_type":"","work_type":""}
{"acceptance_criteria":"","actor":"","agent_state":"","assignee":null,"await_id":"","await_type":"","close_reason":"obgo-sync-local.sh working: port-forward + docker, synced 19 files bidirectionally. Script at ~/Nessei/obgo-sync-local.sh, secrets at ~/Nessei/.obgo-sync.env","closed_at":"2026-04-06T03:51:29Z","closed_by_session":"","compacted_at":null,"compacted_at_commit":null,"compaction_level":0,"content_hash":"4e70d9cf0305e42fb854b36a7fa67d487c02a3329eced8048c7d4d760a3cf3c2","created_at":"2026-04-06T03:38:31Z","created_by":"Woodson Delhia","crystallizes":0,"defer_until":null,"description":"Agents writing vault files locally need changes to reach CouchDB (and then mobile/cluster) without Obsidian desktop being open. Run obgo-sync locally watching the Nessei vault directory, pushing changes to vault.nessei.com CouchDB. Same tool already running on the cluster sidecar. Without this, the entire agent-writes-to-vault workflow is broken when Obsidian is closed.","design":"","due_at":null,"ephemeral":0,"estimated_minutes":null,"event_kind":"","external_ref":null,"hook_bead":"","id":"obsidian-mcp-server-dao","is_template":0,"issue_type":"task","last_activity":null,"metadata":"{}","mol_type":"","no_history":0,"notes":"","original_size":null,"owner":"woodsondelhia@gmail.com","payload":"","pinned":0,"priority":0,"quality_score":null,"rig":"","role_bead":"","role_type":"","sender":"","source_repo":"","source_system":"","spec_id":"","status":"closed","target":"","timeout_ns":0,"title":"Run obgo-sync locally to enable vault sync without Obsidian open","updated_at":"2026-04-06T03:51:29Z","waiters":"","wisp_type":"","work_type":""}
{"acceptance_criteria":"","actor":"","agent_state":"","assignee":"Woodson Delhia","await_id":"","await_type":"","close_reason":"ktp: spawner.string→spawn+exitCode in Git.ts (test/git.test.ts is the regression). 2xp: chain lefthook from .beads/hooks/pre-commit (outside markers); removed lefthook install from prepare. oyd: smoke confirmed from Mattermost. wpq, r98: ADR records, work landed in 412f45f / bacdbbc / earlier — closing as recorded.","closed_at":"2026-04-07T18:28:55Z","closed_by_session":"","compacted_at":null,"compacted_at_commit":null,"compaction_level":0,"content_hash":"5f4ebb4911955b0692b49cdff22a76cb1dda2a604ef7ef43d591f116c2afffce","created_at":"2026-04-07T06:02:28Z","created_by":"Woodson Delhia","crystallizes":0,"defer_until":null,"description":"services/Git.ts uses spawner.string(command) which returns stdout regardless of exit code. If git clone fails (e.g. repo doesn't exist, auth fails, network error), the handler still resolves with the target path as if the clone succeeded. We hit this in production: the cluster pod logged 'ensured repo: obsidian-mcp-server -\u003e /data/repos/obsidian-mcp-server' but the directory was actually empty because the clone target didn't exist. Fix: switch to spawner.spawn() and explicitly check handle.exitCode, OR re-stat the gitDir after the run and fail if it's still missing. Same risk applies to BeadsCli but is masked there because the schema decode fails on the empty-stdout case.","design":"","due_at":null,"ephemeral":0,"estimated_minutes":null,"event_kind":"","external_ref":null,"hook_bead":"","id":"obsidian-mcp-server-ktp","is_template":0,"issue_type":"bug","last_activity":null,"metadata":"{}","mol_type":"","no_history":0,"notes":"","original_size":null,"owner":"woodsondelhia@gmail.com","payload":"","pinned":0,"priority":1,"quality_score":null,"rig":"","role_bead":"","role_type":"","sender":"","source_repo":"","source_system":"","spec_id":"","status":"closed","target":"","timeout_ns":0,"title":"Git.ensureRepo silently swallows clone failures","updated_at":"2026-04-07T18:28:55Z","waiters":"","wisp_type":"","work_type":""}
{"acceptance_criteria":"","actor":"","agent_state":"","assignee":null,"await_id":"","await_type":"","close_reason":"Hook was for syncing bd writes to Obsidian vault. Project repurposed to mattermost-taskbridge; Obsidian is no longer in scope.","closed_at":"2026-04-07T03:51:38Z","closed_by_session":"","compacted_at":null,"compacted_at_commit":null,"compaction_level":0,"content_hash":"e03415f5092c6807cf70231ac99e57668b2f4ddfb2f4e8be06b065ba5688c166","created_at":"2026-04-06T02:51:03Z","created_by":"Woodson Delhia","crystallizes":0,"defer_until":null,"description":"Every bd write (create, update, close, comment) made by agents working locally in Claude Code should reflect in the Obsidian vault. Implement a post_tool_call hook in .claude/settings.json that fires after any Bash(bd:*) command and calls the MCP server to sync the change to the corresponding Obsidian note. The MCP server Beads tools handle this natively, but agents using bd directly need this hook as a safety net.","design":"","due_at":null,"ephemeral":0,"estimated_minutes":null,"event_kind":"","external_ref":null,"hook_bead":"","id":"obsidian-mcp-server-kxz","is_template":0,"issue_type":"task","last_activity":null,"metadata":"{}","mol_type":"","no_history":0,"notes":"","original_size":null,"owner":"woodsondelhia@gmail.com","payload":"","pinned":0,"priority":1,"quality_score":null,"rig":"","role_bead":"","role_type":"","sender":"","source_repo":"","source_system":"","spec_id":"","status":"closed","target":"","timeout_ns":0,"title":"Add Claude Code hook to sync bd commands to Obsidian vault","updated_at":"2026-04-07T03:51:38Z","waiters":"","wisp_type":"","work_type":""}
{"acceptance_criteria":"","actor":"","agent_state":"","assignee":null,"await_id":"","await_type":"","close_reason":"ktp: spawner.string→spawn+exitCode in Git.ts (test/git.test.ts is the regression). 2xp: chain lefthook from .beads/hooks/pre-commit (outside markers); removed lefthook install from prepare. oyd: smoke confirmed from Mattermost. wpq, r98: ADR records, work landed in 412f45f / bacdbbc / earlier — closing as recorded.","closed_at":"2026-04-07T18:28:55Z","closed_by_session":"","compacted_at":null,"compacted_at_commit":null,"compaction_level":0,"content_hash":"5013e90714050eaac4c8481e0e598c60a22b253943d2271636f6263c40152dc9","created_at":"2026-04-07T04:15:41Z","created_by":"Woodson Delhia","crystallizes":0,"defer_until":null,"description":"Build the first vertical slice of mattermost-taskbridge: a Bun HTTP server using Effect v4 unstable/http + BunHttpServer that responds to three Mattermost slash commands (/projects, /board, /task show) by shelling out to bd via BunChildProcessSpawner. Read-only, single-project (this repo) bootstrap. Validates the end-to-end Mattermost -\u003e service -\u003e bd -\u003e response loop before scaling out to interactive actions, dialogs, GitHub webhooks, and k8s deployment. Design field contains the full approved plan.","design":"# Plan: mattermost-taskbridge — Phase 1 thin-slice MVP\n\n## Context\n\nThe repo `/home/woodson/Nessei/obsidian-mcp-server` (recently repurposed to `mattermost-taskbridge`) currently contains only a stub `index.ts`. The team needs a way to view Beads task state across multiple GitHub repos from Mattermost on their phones. The spec the user provided describes the full Phase 1 service; this plan covers a **thin-slice MVP** that proves the end-to-end loop (Mattermost → service → `bd` CLI → response) before scaling out to interactive actions, dialogs, GitHub webhooks, and k8s.\n\nThe MVP must answer two questions the spec under-specified:\n1. **How does a user pick a project to view?** → `/projects` lists what's configured in `projects.toml`. `/board \u003cproject\u003e` filters to one. `/board` (no arg) shows a multi-project summary.\n2. **How does a user view task detail?** → `/task show \u003cbead-id\u003e`. Bead IDs are already namespaced by project (e.g., `obsidian-mcp-server-2pv`), so the service routes the lookup to the right local repo by parsing the prefix and matching it against `projects.toml`. Adding a new project = clone into `REPOS_DIR`, add an entry to `projects.toml`, restart.\n\n## Decisions (locked)\n\n- **Scope**: thin slice — read-only `/projects`, `/board`, `/task show`. No write actions, no dialogs, no webhooks, no Dockerfile, no k8s in this PR.\n- **Stack**: `effect@4.0.0-beta.43` with `effect/unstable/http` (already installed) + `@effect/platform-bun` (`BunHttpServer`, `BunChildProcessSpawner`) — needs to be added as a dep, version aligned to the matching beta.\n- **DI**: services modeled as `ServiceMap.Service` with `Layer.effect` composition (the v4 idiom — verified in `node_modules/effect/src/ServiceMap.ts`).\n- **Shell-out**: `BunChildProcessSpawner` from `@effect/platform-bun`, not raw `Bun.$`. Lets us pipe stdout into Schema-validated JSON parsing and get typed errors.\n- **Config**: env vars via `Config.string`/`Config.redacted`/`Config.integer`; `projects.toml` loaded via Bun's native TOML loader (`import config from \"./projects.toml\"`) inside an `AppConfig` Layer. **Zero TOML dependencies** — Bun parses `.toml` natively at runtime and bundle time.\n- **Mattermost**: live homelab cluster instance. `kubectl --context homelab` shows `mattermost/mattermost-service` (ClusterIP `10.43.41.76:80`, internal DNS `mattermost-service.mattermost.svc.cluster.local`), pod up. Cluster manifests at `~/Code/DevOps/homelab-k8s` (canonical) or `~/Code/projects/homelab-k8s`. Slash commands will be registered via System Console pointing at this service URL once the TaskBridge service is itself deployed to the cluster — for dev iteration on the local box, we'll either tunnel or run a port-forwarded smoke test.\n- **Tests**: `bun test` against this repo's own `.beads/` for happy-path integration; one fixture-based unit test for the Mattermost formatter.\n\n## Beads JSON shape (verified, not from agent guesswork)\n\nConfirmed via `bd list --status=closed --json` against the live repo. Fields are:\n\n```\nid              string   e.g. \"obsidian-mcp-server-2pv\"  (prefix = project key)\ntitle           string\ndescription     string\nstatus          \"open\" | \"closed\"   (NOT \"pending\"/\"completed\" — agent was wrong)\npriority        integer 0–4         (0 = critical)\nissue_type      \"task\" | \"bug\" | \"feature\" | \"chore\" | \"epic\"  (field name is issue_type, not type)\nowner           string              (email)\nassignee        string?             (optional, only set when claimed)\ncreated_at      RFC3339\ncreated_by      string\nupdated_at      RFC3339\nclosed_at       RFC3339?\nclose_reason    string?\ndependency_count    integer\ndependent_count     integer\ncomment_count       integer\n```\n\n`bd ready --json` and `bd list --json` return arrays of this shape. `bd show \u003cid\u003e --json` returns a richer shape including dependency lists; the MVP only needs the same fields plus the dep arrays.\n\nThe status enum needs verification before coding — there's likely an `in_progress` value in the wild. The Schema decoder should accept it via `Schema.Literal(\"open\", \"in_progress\", \"closed\")` and reject everything else loudly.\n\n## File layout\n\n```\nsrc/\n├── main.ts                    # Entry: builds the layer stack, runs BunRuntime\n├── config/\n│   ├── AppConfig.ts           # ServiceMap.Service: env + projects.toml\n│   └── ProjectConfig.ts       # Schema for one project entry\n├── services/\n│   ├── BeadsCli.ts            # ServiceMap.Service: ready/list/show via spawner\n│   └── Git.ts                 # ServiceMap.Service: ensureRepo (clone or pull)\n├── domain/\n│   ├── BeadTask.ts            # Schema.Class for the JSON shape above\n│   └── ProjectKey.ts          # Branded string + parseFromBeadId helper\n├── mattermost/\n│   ├── SlashRequest.ts        # Schema for the inbound slash command form body\n│   ├── Response.ts            # Schema + builders for outbound interactive payloads\n│   └── format.ts              # bd JSON arrays → Mattermost attachment payloads\n├── http/\n│   ├── routes.ts              # HttpRouter wiring for /slash/projects, /slash/board, /slash/task\n│   └── handlers/\n│       ├── projects.ts        # POST /slash/projects\n│       ├── board.ts           # POST /slash/board\n│       └── task.ts            # POST /slash/task\n└── startup/\n    └── ensureRepos.ts         # On boot: ensure each project in projects.toml is cloned + pulled\n\nconfig/\n└── projects.toml              # One entry initially: this repo\n\ntest/\n├── format.test.ts             # Formatter unit test with captured bd JSON fixture\n└── integration.test.ts        # Live BeadsCli against this repo's .beads/\n```\n\n## Service sketches\n\n**`BeadsCli`** — three methods, all delegate to `BunChildProcessSpawner` and decode through `Schema.Array(BeadTask)`:\n\n```ts\nclass BeadsCli extends ServiceMap.Service\u003cBeadsCli, {\n  readonly ready: (repoPath: string) =\u003e Effect.Effect\u003cReadonlyArray\u003cBeadTask\u003e, BeadsCliError\u003e\n  readonly list:  (repoPath: string, opts?: { status?: BeadStatus }) =\u003e Effect.Effect\u003cReadonlyArray\u003cBeadTask\u003e, BeadsCliError\u003e\n  readonly show:  (repoPath: string, id: string) =\u003e Effect.Effect\u003cBeadTaskDetail, BeadsCliError | NotFoundError\u003e\n}\u003e(\"@taskbridge/BeadsCli\") {\n  static readonly layer = Layer.effect(BeadsCli, Effect.gen(function*() {\n    const spawner = yield* BunChildProcessSpawner.BunChildProcessSpawner\n    // ... three methods\n  }))\n}\n```\n\n**`Git`** — `ensureRepo(project)`: if `${REPOS_DIR}/${project.key}` exists, `git -C \u003cpath\u003e pull --rebase --ff-only`; else `git clone --branch \u003cbranch\u003e --single-branch \u003crepo\u003e \u003cpath\u003e`. No commit/push in MVP.\n\n**`AppConfig`** — loads env via `Config.*`, imports `./config/projects.toml` directly using **Bun's native TOML loader** (`import projectsToml from \"./projects.toml\" with { type: \"toml\" }` — no dep needed; verified at https://bun.sh/docs/runtime/loaders), decodes with `Schema.Array(ProjectConfig)`, returns the merged frozen config. A `projects.d.ts` declaration may be needed to silence tsgo on the TOML import.\n\n**`ProjectKey.parseFromBeadId`** — pure helper. Given `\"obsidian-mcp-server-2pv\"` and the list of configured project keys `[\"obsidian-mcp-server\", ...]`, find the longest-prefix match and return `{ projectKey, beadSuffix }`. Longest-prefix is necessary because keys can share prefixes.\n\n## Routes (MVP)\n\n| Method | Path | Behavior |\n|---|---|---|\n| POST | `/slash/projects` | Returns ephemeral message listing each project from `projects.toml` with its display name and ready/open task counts. |\n| POST | `/slash/board` | `text` form field is the project key, optionally followed by `ready` or `blocked`. No arg = summary across all projects. Returns in-channel message with one attachment per task (no buttons in MVP — just title, status, priority, assignee). |\n| POST | `/slash/task` | `text` form field is `show \u003cbead-id\u003e`. Parses the prefix, routes to the right repo, calls `bd show \u003cid\u003e --json`, returns an in-channel message with description, deps, dates. Other subcommands (`create`, `assign`) return \"not yet implemented\" for now. |\n\n### Mattermost protocol (verified against `mattermost-developer-documentation`)\n\n**Request** — Mattermost POSTs `application/x-www-form-urlencoded` to the registered Request URL. Verified field set:\n\n```\nchannel_id, channel_name, command, response_url, team_domain,\nteam_id, text, token, trigger_id, user_id, user_name\n```\n\nPlus an `Authorization: Token \u003ctoken\u003e` header that mirrors the body `token`. The handler decodes the form into a `SlashRequest` Schema, verifies `token` against an env-supplied per-command secret, then dispatches.\n\n**Response timing** — Mattermost waits **3 seconds** by default (`OutgoingIntegrationRequestsDefaultTimeout` in `config.json`). For the MVP, all bd reads against this repo run well under that, so we respond synchronously. The `SlashRequest` Schema captures `response_url` anyway so we can pivot to the delayed-response pattern (immediate ephemeral ack + up to 5 follow-up POSTs to `response_url` within 30 minutes) the first time a cold-disk `bd show` blows the budget.\n\n**Response shape** — JSON with: `response_type` (`in_channel` | `ephemeral`), `text` (Markdown), optional `username`, `icon_url`, `props.attachments` (Slack-compatible attachment list with `color`, `title`, `fields[]`, eventually `actions[]`), `skip_slack_parsing`. MVP omits the `actions` array entirely.\n\n**Streaming** — Not supported. Mattermost has no chunked/SSE pattern; the only async path is `response_url` follow-ups.\n\n**Registration** — System Console → Integrations → Slash Commands → Add Slash Command (custom slash commands must be enabled first). For each command (`/board`, `/projects`, `/task`), set Request URL pointing at the dev box, Method = POST, Trigger word, autocomplete hint. Mattermost generates a token; copy it into the env so the handler can verify.\n\n## Wiring in `src/main.ts`\n\n```ts\nconst MainLayer = Layer.mergeAll(\n  AppConfig.layer,\n  BeadsCli.layer,\n  Git.layer,\n  BunChildProcessSpawner.layer,\n  BunFileSystem.layer,\n).pipe(\n  Layer.provideMerge(BunHttpServer.layer({ port: 3100 }))\n)\n\nconst program = Effect.gen(function*() {\n  yield* ensureRepos              // clone/pull all configured projects\n  yield* HttpServer.serve(routes) // never returns\n})\n\nBunRuntime.runMain(program.pipe(Effect.provide(MainLayer)))\n```\n\n## projects.toml (initial)\n\n```toml\n[[projects]]\nkey = \"obsidian-mcp-server\"\nrepo = \".\"                      # local path; Git layer skips clone for local paths\ndisplay_name = \"TaskBridge (self)\"\ncolor = \"#1D9E75\"\n```\n\nThe `repo = \".\"` shortcut lets the MVP bootstrap with the current repo without needing GitHub auth set up. Real GitHub URLs work the same way once they're added.\n\n## Verification\n\n1. `bun install` and `bun run typecheck` clean (tsgo with Effect Language Service)\n2. `bun run lint` and `bun run fmt:check` clean\n3. `bun test` — formatter unit test passes; integration test against this repo's `.beads/` returns the real `obsidian-mcp-server-*` issues\n4. `bun run src/main.ts` boots, logs `serving on :3100`, and `curl -X POST -d \"text=\u0026user_name=test\" http://localhost:3100/slash/projects` returns a JSON Mattermost payload mentioning `TaskBridge (self)`\n5. `curl -X POST -d \"text=obsidian-mcp-server\u0026user_name=test\" http://localhost:3100/slash/board` returns a payload listing the open beads in this repo\n6. `curl -X POST -d \"text=show obsidian-mcp-server-xfb\u0026user_name=test\" http://localhost:3100/slash/task` returns a payload with that task's detail\n7. **Live smoke test against dev Mattermost**: register `/board`, `/projects`, `/task` slash commands in System Console pointing at the dev box (tunnel/local IP), type `/projects` in any channel, see the attachment render correctly on desktop and mobile\n\n## Out of scope (filed as follow-up beads issues at end of session)\n\n- Interactive button actions (`/actions/update`, `/actions/assign`)\n- Dialog flows (`/actions/dialog`, `/dialogs/submit`) — comment, create task\n- GitHub webhook receiver (`/webhooks/github`) with HMAC verification\n- Background repo-pull poller (MVP only pulls on startup)\n- Write paths: `bd update`, `bd close`, `bd create` from Mattermost + `git commit \u0026\u0026 git push`\n- Status color mapping for `in_progress`, `blocked`\n- `Dockerfile`, `k8s/deployment.yaml`, `k8s/service.yaml`, `k8s/configmap.yaml`, `k8s/secret.yaml`\n- Mattermost `AllowedUntrustedInternalConnections` config note in deployment docs\n- Multi-project bead-ID prefix collision tests (longest-prefix matching edge cases)\n\n## Critical files to read before coding\n\n- `node_modules/effect/src/unstable/http/HttpRouter.ts` — confirm `add(method, path, handler)` signature and handler return type\n- `node_modules/effect/src/unstable/http/HttpServerRequest.ts` — `schemaBodyUrlParams` (form decoding) vs `schemaBodyJson`\n- `node_modules/effect/src/ServiceMap.ts` — current `Service` constructor + `.of` patterns\n- `node_modules/effect/src/Config.ts` — `Config.redacted`, `Config.integer`, fallback patterns\n- `.effect-v4/packages/platform-bun/src/BunHttpServer.ts` — Layer constructor signature, port option\n- `.effect-v4/packages/platform-bun/src/BunChildProcessSpawner.ts` — spawner API, stdout/stderr handling, exit code → error mapping\n- `effect-solutions show services-and-layers config error-handling` — check that the patterns above haven't drifted\n\n## Risks / unknowns to surface during implementation\n\n1. **`@effect/platform-bun` version alignment** — must find a version compatible with `effect@4.0.0-beta.43`. If no published `4.0.0-beta.x` exists, we may need to pin to a git ref from `.effect-v4`. First implementation step is `bun add @effect/platform-bun` and check the resolved version.\n2. **`unstable/http` API churn** — it's literally named `unstable`. The agent's example used method names that may have moved (`router.add` vs `HttpRouter.add(router, ...)`). Read the actual `HttpRouter.ts` source before writing handlers.\n3. **Status enum drift** — verify `in_progress` is or isn't a real bd status by creating a test issue and claiming it.\n4. **`bd show --json` shape** — only sampled list/ready output. The detail shape may differ (extra dep arrays, comments). Sample it before defining `BeadTaskDetail`.","due_at":null,"ephemeral":0,"estimated_minutes":null,"event_kind":"","external_ref":null,"hook_bead":"","id":"obsidian-mcp-server-oyd","is_template":0,"issue_type":"feature","last_activity":null,"metadata":"{}","mol_type":"","no_history":0,"notes":"DEPLOY FULLY WORKING as of commit 412f45f. Mattermost slash commands /projects, /board, /task show all return real bead data in the homelab cluster. Fix was docker/entrypoint.sh that (a) waits for taskbridge's ensureRepos to clone each configured repo, (b) purges stale dolt-server.{lock,pid,port} from the PVC, (c) kills any dolt still holding a lock on the data dir, (d) runs bd init + bd backup restore for fresh clones, (e) runs bd dolt start to bring up exactly one long-lived dolt per project. Subsequent handler subprocess bd invocations find the live dolt via the lock file and reuse it. Also started tracking .beads/backup/*.jsonl via force-add so bd's canonical portable snapshot travels with git clone — the cluster pod restores from those on first boot. Only remaining step: directory rename obsidian-mcp-server → mattermost-taskbridge + Claude Code session restart.","original_size":null,"owner":"woodsondelhia@gmail.com","payload":"","pinned":0,"priority":1,"quality_score":null,"rig":"","role_bead":"","role_type":"","sender":"","source_repo":"","source_system":"","spec_id":"","status":"closed","target":"","timeout_ns":0,"title":"Phase 1 thin-slice MVP: read-only Mattermost slash commands for Beads","updated_at":"2026-04-07T18:28:55Z","waiters":"","wisp_type":"","work_type":""}
{"acceptance_criteria":"","actor":"","agent_state":"","assignee":null,"await_id":"","await_type":"","close_reason":"","closed_at":null,"closed_by_session":"","compacted_at":null,"compacted_at_commit":null,"compaction_level":0,"content_hash":"e5809d41787facf90b0da1a080f07b3cb6722093ebd5c452a6b81e1288be6f63","created_at":"2026-04-07T19:38:37Z","created_by":"Woodson Delhia","crystallizes":0,"defer_until":null,"description":"Decides how mattermost-taskbridge syncs bead state across dev box, cluster pod, and (in Phase 6) Mattermost-initiated writes after the bd v1.0.0 upgrade. v1.0.0 breaks the obsidian-mcp-server-wpq approach because bd backup is no longer JSONL — it's Dolt-native binary. This ADR adopts Architecture B: keep gitops, but migrate the JSONL format from bd backup to bd export/bd import. Phase 6 will add an HTTP convenience layer on top with best-effort merge for conflict resolution. See design field for full investigation, alternatives (centralized Dolt remote rejected; HTTP-only single-writer rejected), and consequences.","design":"# ADR: Architecture B (git canonical via `bd export`/`bd import`) for the bd v1.0.0 migration\n\n**Status**: Accepted (2026-04-07)\n**Context**: bd v1.0.0 upgrade for mattermost-taskbridge. Supersedes part of `obsidian-mcp-server-wpq`.\n**Author**: Woodson Delhia (with Claude Code investigation)\n**Related**: `obsidian-mcp-server-wpq` (the original gitops sync ADR), `obsidian-mcp-server-r98` (rejected beads-mcp pivot)\n\n## Question\n\nTwo intertwined questions:\n\n1. **Sync architecture**: How does bead state move between the dev box, the cluster pod, and (in Phase 6) Mattermost-initiated writes? `obsidian-mcp-server-wpq` answered this for bd 0.61.0 by committing `.beads/backup/*.jsonl` files to git and running `bd backup restore` in the cluster pod's sync loop. **bd v1.0.0 breaks that approach**: `bd backup` is no longer a JSONL exporter — it's a Dolt-native binary backup wrapping `CALL DOLT_BACKUP(...)`. The JSONL files we currently sync stop being produced after upgrade.\n\n2. **Offline workflow**: Do dev writes (`bd close`, `bd update`) work when the dev box is offline / disconnected from the cluster, with sync deferred to reconnection?\n\n## Alternatives considered\n\n### A. Centralized Dolt remote (DoltHub or self-hosted)\n\nA single Dolt SQL server is the canonical store; dev box and cluster pod are clients that `bd dolt push`/`pull`.\n\n- **Reads**: local Dolt cache, sub-second.\n- **Writes**: local first, push when online. Dolt merges concurrent writes at the row level.\n- **Offline**: ✅ writes accumulate locally, sync deferred to `bd dolt push`.\n- **Pros**: True real-time multi-writer with row-level merge. Sync can be sub-second instead of 15-25s.\n- **Cons**: New external dependency (DoltHub account or self-hosted Dolt server). **Git stops being the source of truth** — `obsidian-mcp-server-wpq` chose gitops specifically to avoid this. The git repo would no longer reflect bead state, breaking audit trail and PR-reviewability of bead changes. Dolt push to git-protocol remotes is documented as slow and not recommended; the supported path is DoltHub-protocol gRPC, which requires either DoltHub or running our own DoltHub-compatible service.\n- **Verdict**: rejected. We don't have the multi-writer pressure or sub-second sync requirements that would justify the additional infra and the loss of \"git as canonical.\"\n\n### B. Git canonical, JSONL via `bd export` / `bd import`\n\nSame shape as `obsidian-mcp-server-wpq`'s gitops sync, but migrated from the deprecated `bd backup` JSONL flow to v1.0.0's supported `bd export` / `bd import` JSONL flow. Memories ride along automatically because `bd export` includes them as `{\"_type\":\"memory\",...}` lines (added in 0.62.0).\n\n- **Reads**: local Dolt, ~200ms (long-lived dolt process from `obsidian-mcp-server-wpq`).\n- **Writes**: dev box runs `bd ...`, then `bd export -o .beads/state.jsonl`, `git commit`, `git push`. Cluster pod polls git, runs `bd import .beads/state.jsonl`.\n- **Offline**: ✅ all bd writes are local; git commits work offline; `git push` defers until reconnection.\n- **Pros**: Preserves wpq's \"git is canonical\" property — every bead state change is a git commit, auditable, reviewable. No new infra. The migration is mechanical: ~3 file edits (Dockerfile, entrypoint.sh, dev workflow).\n- **Cons**: 15-25s sync lag (already accepted in wpq). For Phase 6, the cluster pod becomes a git writer with credentials, which means a deploy key or fine-grained PAT in the pod.\n- **Verdict**: **selected**.\n\n### C. HTTP through cluster (single writer, all writes routed through taskbridge)\n\nCluster pod is the only writer. Dev box becomes a thin HTTP client that calls taskbridge endpoints for `bd close`, `bd update`, etc.\n\n- **Offline**: ❌ **fails** — writes require cluster connectivity. Dev workflow stops working on a flaky connection or when the cluster is down.\n- **Verdict**: rejected as a primary architecture for this exact reason. Will be revisited as a *layer on top of B* in Phase 6 (see \"Phase 6 endgame\" below).\n\n## Decision\n\n**Adopt Architecture B for the v1.0.0 migration. Phase 6 will add an HTTP convenience layer on top of B for Mattermost-initiated writes, with best-effort git merge as the conflict resolution strategy.**\n\n### What changes from `obsidian-mcp-server-wpq`\n\n1. **Sync command swap**:\n   - Dev box: `bd backup` → `bd export -o .beads/state.jsonl`\n   - Cluster pod (`docker/entrypoint.sh`): `bd backup restore` → `bd import .beads/state.jsonl` (called from both the boot path and the background sync loop)\n2. **File layout**:\n   - Add: `.beads/state.jsonl` (single file, tracked normally — no force-add needed because `bd export` doesn't write into the gitignored `.beads/backup/` directory)\n   - Remove: `.beads/backup/*.jsonl` from git tracking (and the corresponding `git add -f` step from the dev workflow). Keep `.beads/backup/` gitignored as before.\n3. **Image bump**:\n   - `Dockerfile:18` `ARG BD_VERSION=0.61.0` → `1.0.0`\n   - `Dockerfile:28` URL `steveyegge/beads` → `gastownhall/beads` (the canonical repo; `steveyegge` now 301-redirects)\n   - `Dockerfile:51` drop `--ignore-scripts` from `bun install` (the lefthook fix in commit `e08738b` made it unnecessary)\n4. **Manifest pin**:\n   - `homelab-k8s/manifests/platform/mattermost-taskbridge/taskbridge.deployment.yaml:71` `image: ...:latest` → pinned tag (e.g. `:v0.2.0-bd-1.0.0`) to give us a clean rollback target for this migration\n5. **ADR wpq**: add a \"Revisited 2026-04-07\" entry pointing here. wpq's selected approach is no longer literally true (the JSONL files are now produced by `bd export`, not `bd backup`), but its *principles* — gitops, bounded sync lag, cluster pod ignorant of bd internals beyond a single command, no Dolt remote — are unchanged. This ADR amends wpq, not replaces it.\n\n### Phase 6 endgame: B + HTTP convenience layer\n\nWhen Mattermost interactive buttons / write actions land (the deferred \"Phase 2\"):\n\n- Add `POST /actions/close`, `POST /actions/comment`, etc. to taskbridge.\n- These endpoints write to the cluster pod's local Dolt, then `bd export` + `git commit` + `git push` from inside the pod.\n- Dev box is unaffected — keeps writing locally and pushing via its existing flow.\n- The cluster pod becomes a *second writer*, not a single writer. Two writers, one canonical store (git), with merge happening on `git pull --rebase`.\n- **Conflict strategy: best-effort merge.** Most changes touch different beads / different lines, so git's normal rebase handles them. The 1% of true conflicts (both writers modifying the same bead in the same sync window) get resolved manually by whoever pushed second. We don't add a write lock or pause-the-sync-loop mechanism unless we observe conflicts becoming a real problem in practice.\n- Why not single-writer (full Architecture C)? Because losing offline-capable dev workflow is a worse trade than handling occasional merge conflicts.\n\n## Verification\n\nThis ADR is implemented by epic `mattermost-taskbridge-\u003cid\u003e` (\"bd v1.0.0 migration\"). The migration is verified phase by phase via a verification loop at each step:\n\n1. **Phase 1 (dev box upgrade)**: `bd ready`, `bd show`, `bd memories`, `bun test`, `bun run typecheck` all pass against the migrated `.beads/dolt/`.\n2. **Phase 2 (dev workflow swap)**: `bd export -o .beads/state.jsonl` produces a file containing all current beads + the lefthook memory. `bd import` round-trip on a tarball-snapshot copy of `.beads/dolt/` reproduces the original state byte-for-byte (or as close as Dolt's deterministic format allows).\n3. **Phase 3 (Docker image)**: `docker build` succeeds locally. `docker run` starts the new entrypoint without errors and `bd ready` works inside the container.\n4. **Phase 4 (cluster rollout)**: pod boots cleanly, `bd doctor` migration runs against the existing PVC, slash commands `/projects` `/board` `/task` return real data from Mattermost. Dev-side `bd close \u003ctest-bead\u003e` + push reaches the cluster within ~25s.\n5. **Phase 5 (ADR wpq update)**: wpq's design field gains a revisit entry pointing here.\n\nIf any phase's verification fails, the plan is to roll back to the previous phase (snapshot tarballs, ArgoCD image rollback, git revert) and file an issue describing what broke before continuing.\n\n## Consequences\n\n### Positive\n\n- **Continued gitops**: bead state remains visible, reviewable, and auditable in the GitHub repo.\n- **No new infra**: no Dolt remote, no DoltHub account, no extra cluster service.\n- **Offline-capable dev workflow**: dev writes work disconnected, sync on push.\n- **Memories sync automatically**: `bd export` round-trips them as part of its standard output. The empirical check in `.beads/backup/config.jsonl` already shows memories were riding along under 0.61.0; v1.0.0 makes this explicit and documented.\n- **v1.0.0 features available**: custom statuses, custom types, the `bd export`/`bd import` round-trip path, post-write auto-backup, schema improvements through migration 0023.\n- **Clean rollback target**: pinning the image to a versioned tag means any cluster regression is one ArgoCD revert away.\n\n### Negative\n\n- **Cluster pod will need git push credentials in Phase 6**. Out of scope for this ADR but worth flagging now.\n- **The legacy `.beads/backup/*.jsonl` files become orphaned** after migration. We delete them from git tracking in Phase 2 to avoid confusion. The history remains for archaeology.\n- **Dependency on `bd export`/`bd import` round-trip fidelity**. If a future bd release breaks the round-trip semantics for some entity type, our gitops sync silently loses data. Mitigation: the verification loop in Phase 2 explicitly diffs an export → import → export round-trip on a snapshot copy.\n- **Two writers in Phase 6 means rare merge conflicts**. Accepted; resolution is manual.\n\n## Revisit triggers\n\n1. **Real merge conflicts become frequent in Phase 6**. Switch to a write lock or pause-the-sync-loop mechanism.\n2. **A second cluster service needs bd state with sub-second sync**. Reconsider Architecture A (centralized Dolt remote).\n3. **`bd export`/`bd import` round-trip breaks for some entity type**. File upstream issue; pin bd version until fixed.\n4. **Embedded Dolt becomes mandatory** (i.e. `bd dolt start` is removed from v1.0.0+ in a later release). Then `obsidian-mcp-server-wpq`'s long-lived dolt approach needs a new fix; this ADR's sync mechanism is independent and survives.\n5. **GitHub-side rate limits or push reliability become a problem** for the Phase 6 cluster pod. Consider switching the cluster's canonical store to a self-hosted gitea or a Dolt remote.\n","due_at":null,"ephemeral":0,"estimated_minutes":null,"event_kind":"","external_ref":null,"hook_bead":"","id":"obsidian-mcp-server-pin","is_template":0,"issue_type":"decision","last_activity":null,"metadata":"{}","mol_type":"","no_history":0,"notes":"","original_size":null,"owner":"woodsondelhia@gmail.com","payload":"","pinned":0,"priority":1,"quality_score":null,"rig":"","role_bead":"","role_type":"","sender":"","source_repo":"","source_system":"","spec_id":"","status":"open","target":"","timeout_ns":0,"title":"ADR: Architecture B (git canonical via bd export/import) for v1.0.0 migration","updated_at":"2026-04-07T19:38:37Z","waiters":"","wisp_type":"","work_type":""}
{"acceptance_criteria":"","actor":"","agent_state":"","assignee":null,"await_id":"","await_type":"","close_reason":"ktp: spawner.string→spawn+exitCode in Git.ts (test/git.test.ts is the regression). 2xp: chain lefthook from .beads/hooks/pre-commit (outside markers); removed lefthook install from prepare. oyd: smoke confirmed from Mattermost. wpq, r98: ADR records, work landed in 412f45f / bacdbbc / earlier — closing as recorded.","closed_at":"2026-04-07T18:28:55Z","closed_by_session":"","compacted_at":null,"compacted_at_commit":null,"compaction_level":0,"content_hash":"d8cbcf3037581fe86304787cfa6a5b7aebd8884a87bc2cfe8f32c14179783dac","created_at":"2026-04-07T05:11:32Z","created_by":"Woodson Delhia","crystallizes":0,"defer_until":null,"description":"Investigated beads-mcp as a replacement for the BeadsCli wrapper in mattermost-taskbridge. Verdict: do NOT pivot. The CLI direct path is the right choice for this service. See design field for the full investigation.","design":"# ADR: don't proxy beads-mcp from TaskBridge — use bd CLI directly\n\n**Status**: Accepted (2026-04-07)\n**Context**: mattermost-taskbridge (Phase 1 thin-slice MVP, see obsidian-mcp-server-oyd)\n**Author**: Woodson Delhia (with Claude Code investigation)\n\n## Question\n\nShould the Bun service that fronts Mattermost slash commands talk to bd via:\n\n1. **Direct CLI** — shell out to `bd \u003csubcommand\u003e --json` per request and parse stdout (what `BeadsCli.ts` currently does)\n2. **beads-mcp proxy** — connect to the official `beads-mcp` Python MCP server (steveyegge/beads/integrations/beads-mcp) over stdio and call its tools\n\n## Decision\n\n**Use the direct CLI path. Do not pivot to beads-mcp.**\n\n## Forces considered\n\n### What pushed us toward beads-mcp initially\n\n- Already exists, official, in the beads repo — no custom code to maintain\n- Multi-repo support is first-class via the per-request `workspace_root` argument and `BEADS_WORKING_DIR` env var\n- Full tool surface: `init, create, list, ready, show, update, close, dep, blocked, stats, reopen, set_context, admin` — covers Phase 1 reads AND eventual Phase 4 writes\n- Versioned protocol — when bd evolves, beads-mcp tracks it for us\n- Avoids us re-implementing CLI flag knowledge\n\n### What killed it\n\n**1. Latency: ~6.4 seconds per warm tool call.** Mattermost's slash command timeout is 3 seconds. This is a hard miss.\n\nMeasured timings (probe at `scripts/probe-beads-mcp.ts`, since deleted):\n\n```\n[ connect           ]    fast (one-time)\n[ tools/list cold   ]    fast\n[ tools/list warm   ]    fast\n[ call list cold    ]    ~6.4 s\n[ call list warm 1  ]    6383.6 ms\n[ call list warm 2  ]    6389.2 ms\n[ call ready warm   ]    6465.0 ms\n[ call show warm    ]    6423.9 ms\n```\n\nEach tool call spins up a fresh Dolt server connection. The README's note \"Each workspace gets its own Dolt server connection\" was load-bearing — that connection setup is the cost. Beads-mcp uses persistent stdio + FastMCP, so the JSON-RPC layer is fast; the Dolt session is what's slow.\n\nFor comparison, the existing `BeadsCli` calls `bd list --json` directly and returns in **under 100 ms**. That's a 60× regression we cannot paper over.\n\n**2. The \"thin proxy\" wouldn't actually be thin.** Switching paths would have required:\n\n- A new `BeadsMcpClient.ts` (~150 lines: SDK client, lifecycle, error mapping, tool name dispatch)\n- Two separate Schemas because `list`/`ready` return a TRIMMED subset (only `id, title, status, priority, issue_type, assignee, labels, dependency_count, dependent_count` — NO `description`, `owner`, `created_at`, `updated_at`, `closed_at`, `comment_count`) while `show` returns the full rich shape. Our existing single `BeadTask` schema would split into `BeadTaskSummary` + `BeadTaskDetail`.\n- A response unwrapper for FastMCP's double-wrapping (`content[].text` for display, `structuredContent.result` for typed access)\n- Pagination handling: beads-mcp adds `--limit 20` by default to `bd list`, no way to disable from the tool args we saw\n- Workaround for the 3s timeout: would have to use Mattermost's `response_url` delayed-response pattern (immediate ephemeral ack + async followup POST), which is strictly worse UX than the synchronous response we have now\n\n**3. Deployment surface grows.** Container would need: bun + python + uv + bd binary + git, instead of just bun + bd + git. Bigger image, more attack surface, slower startup.\n\n**4. No actual code-to-maintain savings.** We would have replaced ~80 lines of `BeadsCli` with ~150 lines of MCP client + adapters. Write paths in beads-mcp aren't free either — when we add `bd close`, it's a one-line addition to `BeadsCli` either way.\n\n### What we kept from the investigation\n\n- `@modelcontextprotocol/sdk` works on Bun out of the box. Useful reference for any future MCP work in this codebase, even if not for this service.\n- Mattermost Agents (the bundled MCP-aware plugin, available v10.3+) is **DM-only** today per the docs: \"Currently integrations are limited to direct messages between users and the agents.\" That means it can't replace TaskBridge for the public-channel slash command UX the spec required. Worth re-checking after Mattermost lifts that limitation.\n- beads-mcp's tool surface and response shapes are documented above for the next person who looks at this.\n- Mattermost Agents docs: \"The plugin currently doesn't render Markdown links...URLs are displayed in plain text rather than as clickable Markdown-rendered links.\" Rich attachment cards (which we built in `format.ts`) are not reproducible via the Agents path today.\n\n## Consequences\n\n### Positive\n\n- TaskBridge stays at sub-100ms per request\n- Single-language deployment (just Bun + bd binary + git in the Docker image)\n- Phase 4 (interactive Approve/Reject buttons on agent plans) remains feasible because we own the HTTP layer end to end\n- Public-channel slash command UX with rich attachment cards is preserved\n\n### Negative\n\n- We hand-wrap each new bd subcommand we want to expose (~5 lines per method in `BeadsCli`)\n- We don't get bd CLI evolution for free; if bd changes flag names we have to update\n- We don't benefit from beads-mcp's context-window-aware response trimming (but we don't need it — we format for human display, not LLM context)\n\n## Revisit triggers\n\nPivot back to beads-mcp (or some other shared integration) if any of the following become true:\n\n1. **beads-mcp adds connection pooling / persistent Dolt session reuse** so warm tool calls drop below ~200ms\n2. **Mattermost Agents lifts the DM-only restriction** so the plugin can be invoked from public channels with rich rendering — at which point we should reconsider whether TaskBridge needs to exist at all\n3. **bd adds a meaningful new feature** (e.g. cross-repo dependency graph queries) that beads-mcp wraps cleanly but is painful to call directly via flags\n4. **We need to share the beads integration with another service** in the cluster — at that point a real network-accessible MCP server (HTTP/SSE transport) becomes worth running, and TaskBridge would call it as one of multiple clients\n\n## Related\n\n- `obsidian-mcp-server-oyd` — Phase 1 MVP (parent feature)\n- Mattermost MCP Server docs: https://docs.mattermost.com/agents/mcpserver/README.html\n- Mattermost Agents Admin Guide: https://docs.mattermost.com/administration-guide/configure/agents-admin-guide.html\n- beads-mcp README: https://github.com/steveyegge/beads/blob/main/integrations/beads-mcp/README.md\n- @modelcontextprotocol/sdk: https://www.npmjs.com/package/@modelcontextprotocol/sdk","due_at":null,"ephemeral":0,"estimated_minutes":null,"event_kind":"","external_ref":null,"hook_bead":"","id":"obsidian-mcp-server-r98","is_template":0,"issue_type":"decision","last_activity":null,"metadata":"{}","mol_type":"","no_history":0,"notes":"","original_size":null,"owner":"woodsondelhia@gmail.com","payload":"","pinned":0,"priority":2,"quality_score":null,"rig":"","role_bead":"","role_type":"","sender":"","source_repo":"","source_system":"","spec_id":"","status":"closed","target":"","timeout_ns":0,"title":"ADR: don't proxy beads-mcp from TaskBridge — use bd CLI directly","updated_at":"2026-04-07T18:28:55Z","waiters":"","wisp_type":"","work_type":""}
{"acceptance_criteria":"","actor":"","agent_state":"","assignee":null,"await_id":"","await_type":"","close_reason":"Smoke test bead — confirmed the dev→cluster background sync loop by observing this bead propagate from bd create + git push to /board in the homelab Mattermost within ~15s, no pod restart required. See ADR obsidian-mcp-server-wpq for the full design.","closed_at":"2026-04-07T17:58:58Z","closed_by_session":"","compacted_at":null,"compacted_at_commit":null,"compaction_level":0,"content_hash":"49df0a07740432498582d0d6346ae15e08f49ed9b43c727108408a431171930b","created_at":"2026-04-07T17:17:05Z","created_by":"Woodson Delhia","crystallizes":0,"defer_until":null,"description":"If you can see this in /board on the cluster without anyone having restarted the pod, the background git-pull + bd backup restore loop is working.","design":"","due_at":null,"ephemeral":0,"estimated_minutes":null,"event_kind":"","external_ref":null,"hook_bead":"","id":"obsidian-mcp-server-t3i","is_template":0,"issue_type":"task","last_activity":null,"metadata":"{}","mol_type":"","no_history":0,"notes":"","original_size":null,"owner":"woodsondelhia@gmail.com","payload":"","pinned":0,"priority":3,"quality_score":null,"rig":"","role_bead":"","role_type":"","sender":"","source_repo":"","source_system":"","spec_id":"","status":"closed","target":"","timeout_ns":0,"title":"Sync smoke test: should appear in cluster within 30s","updated_at":"2026-04-07T17:58:58Z","waiters":"","wisp_type":"","work_type":""}
{"acceptance_criteria":"","actor":"","agent_state":"","assignee":null,"await_id":"","await_type":"","close_reason":"ktp: spawner.string→spawn+exitCode in Git.ts (test/git.test.ts is the regression). 2xp: chain lefthook from .beads/hooks/pre-commit (outside markers); removed lefthook install from prepare. oyd: smoke confirmed from Mattermost. wpq, r98: ADR records, work landed in 412f45f / bacdbbc / earlier — closing as recorded.","closed_at":"2026-04-07T18:28:55Z","closed_by_session":"","compacted_at":null,"compacted_at_commit":null,"compaction_level":0,"content_hash":"d4321b4a2eedc714552ca7f49163f69b779822b19fecd7e2353b95ab582d7a1b","created_at":"2026-04-07T16:58:07Z","created_by":"Woodson Delhia","crystallizes":0,"defer_until":null,"description":"How mattermost-taskbridge runs bd in a container: pre-start exactly one long-lived dolt per configured project at container boot via a shell entrypoint wrapper, and track bd's .beads/backup/*.jsonl files in git as the canonical dev→cluster sync format. Ships the per-request dolt lock contention fix and the gitops data flow. See design for the full investigation and rationale.","design":"# ADR: long-lived dolt via entrypoint wrapper + gitops sync via background pull loop\n\n**Status**: Accepted (2026-04-07)\n**Context**: mattermost-taskbridge Phase 1 deploy (`obsidian-mcp-server-oyd`)\n**Author**: Woodson Delhia (with Claude Code diagnosis)\n**Supersedes**: None. This is the durable fix for the bd-in-cluster data layer problem that blocked `/board` from returning real data in the initial homelab deploy. Later revised to include the background sync loop that removed the \"restart-required\" friction.\n\n## Question\n\nTaskBridge's slash command handlers (`src/http/handlers/{projects,board,task}.ts`) invoke `bd` via `BunChildProcessSpawner` as a fresh subprocess per request. bd expects a running Dolt SQL server to answer its queries. How do we make this work end-to-end in a k3s pod on arm64 where:\n\n1. The Docker image must ship a working bd + dolt + git toolchain\n2. The cluster pod must reconstruct the bead database on first boot from whatever is in the cloned GitHub repo (no kubectl cp, no Dolt remote)\n3. Per-request bd invocations from the handler must return real data within Mattermost's 3-second slash command budget\n4. Updates made on the dev box must reach the cluster **without requiring a pod restart**\n5. The fix must survive container restarts without manual intervention\n\n## The problem, diagnosed precisely\n\nBooting a fresh pod and calling `/projects` produced `Open: 0  Ready: 0`. `/board` returned a 500. `bd list` manually via `kubectl exec` worked the first time but subsequent calls hung. The smoking gun was `.beads/dolt-server.log` in the pod:\n\n```\nStarting server with Config HP=\"127.0.0.1:36571\"|...\ndatabase \"dolt\" is locked by another dolt process; either clone the database\nto run a second server, or stop the dolt process which currently holds an\nexclusive write lock on the database\n[...this pair repeated 14+ times with different random ports...]\n```\n\nChain of failures:\n\n1. The handler calls `BeadsCli.list`, which spawns `bd list --json --status open` via `spawner.string(command)`.\n2. bd is a CLI client that expects a running Dolt SQL server. On first invocation it auto-spawns dolt in the background.\n3. Dolt comes up, grabs an **exclusive write lock** on `.beads/dolt/`, accepts connections, returns data.\n4. bd prints the results and exits. **Dolt stays running** — it's designed to be persistent.\n5. In principle, a second bd call should read `.beads/dolt-server.lock` to find the live port and reconnect. But in our setup the lock file was being left empty (0 bytes) after the first call. Root cause unclear; likely a subtle race between bd's fork-and-write and container filesystem semantics.\n6. Every subsequent bd invocation therefore tried to spawn a **new** dolt. Each new dolt failed immediately on the lock check (the original dolt still held the exclusive lock), bd's wrapper waited 10 s for the port to come up, then timed out.\n7. From the handler's perspective: bd hangs or errors. `/projects` swallowed the error via `Effect.orElseSucceed(() =\u003e [])` and returned `0/0`. `/board` lacked that swallow and 500'd.\n\nTwo secondary complications made it worse:\n\n- **Zombie dolt accumulation**: every failed auto-start left a dolt process parented to tini, each holding locks on random ports.\n- **Gitignored data**: `.beads/dolt/` is binary and gitignored, so even if the lifecycle worked, a fresh cluster clone had no data to serve.\n\n## Alternatives considered\n\n1. **Switch to bd no-db mode** (`no-db: true` in config.yaml, read JSONL directly). — **Rejected** because it's documented but not actually implemented in bd 0.61.0. Verified: even with `no-db: true` and the dolt directory deleted, bd still tried to open a Dolt server and errored.\n\n2. **Run dolt as a sidecar container** in the same pod, sharing the PVC. — **Rejected** as overkill for a single-project first deploy. Adds yaml, adds a second container lifecycle to reason about, doesn't compose well when taskbridge wants to clone repos lazily via `ensureRepos`.\n\n3. **Use an initContainer** that clones + runs `bd dolt start`. — **Rejected** because initContainers terminate before the main container starts, so the dolt process would die with them.\n\n4. **Long-lived dolt via entrypoint shell wrapper, started after taskbridge clones.** — **Selected**. See the design below.\n\n5. **Modify `src/startup/ensureRepos.ts`** to call `bd dolt start` via the Effect spawner. — Plausible alternative. Rejected in favor of the shell wrapper because: (a) the spawner isn't well-suited to fire-and-forget long-running subprocesses, (b) shell is more appropriate for process-level plumbing like `pkill` and lock file cleanup, (c) keeping taskbridge ignorant of the bd server lifecycle means we can drop the wrapper entirely if bd ever fixes the auto-start reliability.\n\n6. **`bd backup restore` as the cluster bootstrap mechanism** — beads' built-in portable JSONL format, auto-written on every bd write. — **Selected** for data bootstrapping. `.beads/.gitignore` excludes `backup/` by default with an explicit warning against negation patterns. Worked around by `git add -f` on the specific JSONL files so they become tracked despite the gitignore pattern.\n\n7. **Per-request `bd import` / `bd backup restore`** to sync on every slash command. — **Rejected** because every bd operation has a ~7 s floor from dolt connection overhead. Even `bd import --dry-run` takes 6.7 s on a no-op. Blows Mattermost's 3 s slash command budget.\n\n8. **Background polling loop with cheap git-based idempotency check.** — **Selected** as the sync mechanism. See the \"Background sync\" section below.\n\n## Decision\n\n**Ship a small shell entrypoint wrapper (`docker/entrypoint.sh`) that:**\n1. **Pre-starts exactly one long-lived dolt server per configured project** at container boot\n2. **Tracks `.beads/backup/*.jsonl` in git** (force-added past the gitignore) as the dev→cluster data sync format\n3. **Runs a background loop** that polls git for new commits and reconstructs the pod's dolt database whenever the remote moves ahead\n\n### The entrypoint wrapper\n\n`docker/entrypoint.sh`, invoked via tini as the container's PID 1 workload:\n\n**Boot sequence:**\n1. Launch `/app/taskbridge` in the background. taskbridge's existing `ensureRepos` does the git clone/pull as before.\n2. For each project key in `/app/config/projects.json`, poll for `/data/repos/\u003ckey\u003e/.beads/` to appear (up to `BOOT_TIMEOUT` seconds, default 60).\n3. Once the repo clone exists, for each cloned project:\n   - `cd` into the repo\n   - Delete stale `.beads/dolt-server.{lock,pid,port}` files from a previous container lifetime\n   - `pkill -9 -f \"dolt.*$repo/.beads/dolt\"` to kill any dolt still holding a lock\n   - If `.beads/dolt/` is missing (fresh clone), run `bd init --prefix \u003ckey\u003e` followed by `bd backup restore` to reconstruct the database from the committed JSONL snapshots\n   - Run `bd dolt start` to bring up exactly one long-lived dolt server for this project\n4. Fork the **background sync loop** (see next section)\n5. `wait` on the taskbridge PID so tini forwards SIGTERM cleanly through the wrapper\n\nSubsequent bd invocations from the slash command handler find the populated lock file with the live port and reuse the long-lived dolt.\n\n### Background sync loop\n\nAdded in a follow-up commit once the lifecycle fix proved out. The loop runs alongside taskbridge (not blocking boot):\n\n```sh\n(\n  while true; do\n    sleep \"$SYNC_INTERVAL\"           # default 15s\n    for key in $(project_keys); do\n      cd \"$REPOS_DIR/$key\"\n      git fetch --quiet origin main  # ~500ms\n      if [ \"$(git rev-parse HEAD)\" != \"$(git rev-parse origin/main)\" ]; then\n        echo \"[sync] $key: $local -\u003e $remote\"\n        git pull --ff-only --quiet \u0026\u0026 bd backup restore\n      fi\n    done\n  done\n) \u0026\n```\n\n**Why the git rev-parse short-circuit matters**: bd itself has no cheap \"is there new data?\" check — every bd read costs ~7 s (dolt connection overhead, even for dry runs). But `git fetch` is ~500 ms and `git rev-parse` is ~40 ms. So we use git as the idempotency check and pay the 7 s bd cost **only when git tells us there's actually new data**. Most cycles are a ~500 ms no-op.\n\n**Why we can't sync per-request**: we'd love to call `bd backup restore` (or `bd import`) before every slash command handler response, but the 7 s floor blows Mattermost's 3 s budget every time. The polling loop gives us bounded lag (15-30 s) without blocking the request path.\n\n**Safety**:\n- Only runs `git pull --ff-only`, so a force-push won't rewrite history the pod was relying on\n- Handler reads from the live dolt, never from the JSONL directly, so mid-restore reads are either fully old-snapshot or fully new-snapshot (no partial state)\n- A Phase 2 Mattermost-side write path would need to commit + push the backup JSONLs back to git, otherwise this loop overwrites pod-local writes on the next cycle\n\n### Dockerfile changes\n\n- Install `procps` in the runtime stage so `pkill -f` is available (busybox's built-in pkill lacks `-f`)\n- `COPY --chmod=0755 docker/entrypoint.sh /app/entrypoint.sh`\n- Change `CMD` from `[\"/app/taskbridge\"]` to `[\"/app/entrypoint.sh\"]`\n- Keep tini as PID 1 via the unchanged `ENTRYPOINT`\n\n### Data sync via force-added backup JSONLs\n\nbd's `.beads/backup/` directory contains the canonical portable snapshot of the database, auto-written on every write operation:\n\n| File | Content |\n|---|---|\n| `issues.jsonl` | One issue per line, full shape |\n| `dependencies.jsonl` | The dependency graph edges |\n| `events.jsonl` | Audit log (append-only) |\n| `comments.jsonl` | Issue comments |\n| `labels.jsonl` | Issue labels |\n| `config.jsonl` | bd config keys stored in the database |\n| `backup_state.json` | High-water marks for incremental export |\n\n`.beads/.gitignore` excludes `backup/` by default with a comment warning against negation patterns. Workaround: `git add -f` on the specific files, which bypasses the gitignore at add time. Once tracked, git continues to pick up their modifications without needing the `-f` flag again.\n\n## Data flow (end-to-end)\n\n```\ndev box                             GitHub                          cluster pod\n─────────                           ──────                          ───────────\nbd create/update/close\n  └→ writes to local .beads/dolt\n      └→ refresh backup JSONLs:\n         bd backup\n          └→ updates .beads/backup/*.jsonl\n              └→ git add -f + commit + push\n                                    └→ main updated\n                                                                    (~15s later, polling loop wakes)\n                                                                    git fetch origin main         [~500ms]\n                                                                    git rev-parse HEAD != origin  [~40ms]\n                                                                    git pull --ff-only            [~300ms]\n                                                                    bd backup restore             [~7s]\n                                                                    └→ local dolt updated\n                                                                    \n                                                                    (on next slash command)\n                                                                    handler → bd list --json\n                                                                      └→ reuses long-lived dolt    [~200ms]\n                                                                          └→ returns fresh data\n```\n\n**Total dev→Mattermost lag**: 15-25 s average (polling interval + restore time), bounded by `SYNC_INTERVAL`.\n\n## Verification\n\nConfirmed end-to-end with a live smoke test:\n\n1. From a fresh pod boot, `/projects` returns `Open: N Ready: N` with real bead data (no manual exec fixup needed)\n2. `/board obsidian-mcp-server` returns N attachment cards with real bead titles and metadata\n3. `/task show obsidian-mcp-server-oyd` returns the rich detail card including description\n4. Dev-side: `bd create` + `bd backup` + `git add -f + commit + push`\n5. Within ~15 s of the push, the pod's sync loop logs `[sync] obsidian-mcp-server: \u003cold-sha\u003e -\u003e \u003cnew-sha\u003e` followed by `[sync] restore complete`\n6. Next `/board` call from Mattermost shows the new bead without any pod restart\n\n## Consequences\n\n### Positive\n\n- **Real bead data in Mattermost slash commands**, no manual fixup required\n- **Gitops-native sync with bounded lag**. Dev commits appear in the cluster within 15-25 s. No pod restart needed, no Dolt remote, no webhook, no kubectl cp.\n- **Handler requests stay fast** (~200 ms p50). All the expensive bd work happens in the background loop, never on the request path.\n- **Each request-path bd call is a cheap dolt query**, not a dolt startup. The ~7 s connection floor only matters for the background loop, which isn't latency-sensitive.\n- **taskbridge stays ignorant of bd internals**. The lifecycle fix is 100% in the shell wrapper. If bd upstream ever fixes its auto-start reliability, we drop `docker/entrypoint.sh` with zero TS code changes.\n- **Each project gets its own dolt server on its own port**. The wrapper scales naturally to multi-project setups — just add more entries to `projects.json`.\n- **Crash-loop safe**: the entrypoint cleans stale state and starts fresh dolts every time, so accumulated zombies never break future boots.\n\n### Negative\n\n- **Sync is one-way**: dev → cluster. Writes initiated from Mattermost (Phase 2) would need commit-and-push logic to travel back to git; otherwise this loop clobbers them on the next cycle. Acknowledged trade-off, not fixed here.\n- **`.beads/backup/*.jsonl` generates git noise**. Every bd write updates the backup files, and those show up as modifications in `git status`. Trade-off is deliberate — they ARE the data.\n- **~15 s sync lag**. Bounded but not instant. For a chat workflow this is fine; for anything latency-sensitive it's not. Tunable via `SYNC_INTERVAL` env var.\n- **Multi-project scaling limit**. Each project gets its own dolt server binding an ephemeral port and holding memory. At ~10 projects this fits in 8 GB; beyond that, consider a single shared Dolt server via the `--server-host` bd config.\n- **procps added to the image**. Small (~500 KB) but a new runtime dep.\n- **Background loop assumes single replica**. We deploy `replicas: 1` with `strategy: Recreate` (PVC is RWO anyway). Two replicas would race on git pull + dolt restore; need leader election or a different data model.\n\n## Revisit triggers\n\n1. **bd fixes auto-start reliability** upstream so subprocess bd invocations can spawn and reuse dolt cleanly. If that happens, drop `docker/entrypoint.sh` entirely and go back to `CMD [\"/app/taskbridge\"]`.\n2. **bd ships real no-db / JSONL-only mode**. If that lands, drop dolt entirely (~38 MB image reduction), drop the entrypoint wrapper, and use the JSONL as the canonical runtime format.\n3. **bd connection overhead drops below ~500 ms per call** (e.g. connection pooling, persistent session reuse). Then we can afford per-request sync and delete the background loop.\n4. **Phase 2 Mattermost write path** requires bd writes from the cluster to flow back to git. At that point add a post-write hook that commits + pushes the backup files. Design note: we'll want to disable the sync loop during the push window to avoid races.\n5. **Multi-project deployments with \u003e10 repos** where many dolt servers become a memory problem. Switch to a single shared Dolt server via bd's `--server-host` config.\n6. **A bd crash on any project hangs the pod boot**. Today the entrypoint runs each project's dolt setup sequentially; if `bd dolt start` ever hangs, the loop never finishes. Consider per-project timeouts with continue-on-failure.\n\n## Related\n\n- `obsidian-mcp-server-oyd` — Phase 1 MVP (parent feature)\n- `obsidian-mcp-server-r98` — earlier ADR rejecting the beads-mcp proxy path\n- `obsidian-mcp-server-290` — the specific bug this fix closes (bd-in-container dolt server lifecycle)\n- `obsidian-mcp-server-ktp` — `Git.ensureRepo` silently swallows clone failures (still open, orthogonal)\n- `obsidian-mcp-server-2xp` — lefthook/beads-hooks conflict (still open, orthogonal)\n- Commits on `Woody88/mattermost-taskbridge`:\n  - `412f45f` — entrypoint wrapper + force-added backup JSONLs + Dockerfile update\n  - `bacdbbc` — background sync loop added\n- Image: `ghcr.io/woody88/mattermost-taskbridge:latest`\n- `docker/entrypoint.sh` — the wrapper itself, fully commented","due_at":null,"ephemeral":0,"estimated_minutes":null,"event_kind":"","external_ref":null,"hook_bead":"","id":"obsidian-mcp-server-wpq","is_template":0,"issue_type":"decision","last_activity":null,"metadata":"{}","mol_type":"","no_history":0,"notes":"","original_size":null,"owner":"woodsondelhia@gmail.com","payload":"","pinned":0,"priority":2,"quality_score":null,"rig":"","role_bead":"","role_type":"","sender":"","source_repo":"","source_system":"","spec_id":"","status":"closed","target":"","timeout_ns":0,"title":"ADR: long-lived dolt via entrypoint wrapper + gitops sync via background pull loop","updated_at":"2026-04-07T18:28:55Z","waiters":"","wisp_type":"","work_type":""}
{"acceptance_criteria":"","actor":"","agent_state":"","assignee":"Woodson Delhia","await_id":"","await_type":"","close_reason":"Implemented PostToolUse hook as Bun script at .claude/hooks/bd-sync-reminder.ts with project-level settings.json. All write subcommands trigger reminder, read-only commands are silent.","closed_at":"2026-04-06T06:11:34Z","closed_by_session":"","compacted_at":null,"compacted_at_commit":null,"compaction_level":0,"content_hash":"ecfab68c274b35378ad687bbda1d9b8911a4685d1ac3cd75b7533cd148f75c17","created_at":"2026-04-06T02:59:28Z","created_by":"Woodson Delhia","crystallizes":0,"defer_until":null,"description":"Add a PostToolUse hook in .claude/settings.json that fires after any Bash(bd *) command and echoes a reminder to update the corresponding Obsidian note in the Nessei vault. This is a trust-but-verify approach — the AI gets a reminder, not an automated script.","design":"## Implementation Plan\n\n### 1. Bun hook script (.claude/hooks/bd-sync-reminder.ts)\n- Reads PostToolUse JSON from stdin via Bun.stdin.text()\n- Extracts tool_input.command, regex matches bd \u003csubcommand\u003e\n- Checks against Set of write operations (create, update, close, reopen, delete, etc.)\n- Special case: comments add = write, comments \u003cid\u003e = read\n- Extracts bead ID from command or output\n- Prints reminder with vault path (from NESSEI_VAULT env var)\n- Exits 0 silently on non-write commands or parse errors\n\n### 2. Project settings (.claude/settings.json)\n- PostToolUse hook with matcher: Bash\n- Runs: bun .claude/hooks/bd-sync-reminder.ts\n- Sets NESSEI_VAULT env var\n- Project-scoped only (no global settings edit)","due_at":null,"ephemeral":0,"estimated_minutes":null,"event_kind":"","external_ref":null,"hook_bead":"","id":"obsidian-mcp-server-xfb","is_template":0,"issue_type":"task","last_activity":null,"metadata":"{}","mol_type":"","no_history":0,"notes":"","original_size":null,"owner":"woodsondelhia@gmail.com","payload":"","pinned":0,"priority":1,"quality_score":null,"rig":"","role_bead":"","role_type":"","sender":"","source_repo":"","source_system":"","spec_id":"","status":"closed","target":"","timeout_ns":0,"title":"Add post_tool_call hook to remind agents to sync bd changes to Obsidian vault","updated_at":"2026-04-06T06:11:34Z","waiters":"","wisp_type":"","work_type":""}
